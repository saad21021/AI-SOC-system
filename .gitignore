.env
.venv/
__pycache__/
.ipynb_checkpoints/
.streamlit/
*.pyc
.DS_Store
# We include .pkl files because they are the trained models needed for the app
# But we exclude large datasets if they exceed 50MB (sample_network_traffic.csv is small, so we'll keep it)
